<?php
// Auto generated by the build:migrations command

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class CreateLeaderboardJackpotTable extends Migration
{
	/**
	 * Run the migrations.
	 *
	 * @return void
	 */
	public function up()
	{
		// Make enums work pre laravel 10
		Schema::getConnection()->getDoctrineConnection()->getDatabasePlatform()->registerDoctrineTypeMapping("enum", "string");

		$indexes = $this->collectIndexedColumns();
		$columns = Schema::getColumnListing("leaderboard_jackpot");

		$func = Schema::hasTable("leaderboard_jackpot") ? "table" : "create";

		Schema::$func("leaderboard_jackpot", function (Blueprint $table) use ($columns, $indexes) {
			!in_array("id", $columns) && $table->integer("id")->autoIncrement();
			!in_array("character_id", $columns) && $table->integer("character_id")->nullable();
			!in_array("wins", $columns) && $table->integer("wins")->nullable()->default("0");
			!in_array("money_won", $columns) && $table->integer("money_won")->nullable()->default("0");
			!in_array("money_lost", $columns) && $table->integer("money_lost")->nullable()->default("0");
			!in_array("items_gambled", $columns) && $table->integer("items_gambled")->nullable()->default("0");

			!in_array("character_id", $indexes) && $table->index("character_id");
		});
	}

	/**
	 * Reverse the migrations.
	 *
	 * @return void
	 */
	public function down()
	{
		Schema::dropIfExists("leaderboard_jackpot");
	}

	/**
	 * Collect all columns that are indexed in the leaderboard_jackpot table.
	 *
	 * @return array
	 */
	private function collectIndexedColumns(): array
	{
		if (!Schema::hasTable("leaderboard_jackpot")) {
			return [];
		}

		$sm  = Schema::getConnection()->getDoctrineSchemaManager();
		$tbl = $sm->introspectTable("leaderboard_jackpot");

		$indexes = array_reduce($tbl->getIndexes(), function ($carry, $index) {
			return array_merge($carry, $index->getColumns());
		}, []);

		return array_values(array_unique($indexes));
	}
}