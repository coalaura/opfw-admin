<?php
// Auto generated by the build:migrations command

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class CreateMagazinesTable extends Migration
{
	/**
	 * Run the migrations.
	 *
	 * @return void
	 */
	public function up()
	{
		// Make enums work pre laravel 10
		Schema::getConnection()->getDoctrineConnection()->getDatabasePlatform()->registerDoctrineTypeMapping("enum", "string");

		$indexes = $this->collectIndexedColumns();
		$columns = Schema::getColumnListing("magazines");

		$func = Schema::hasTable("magazines") ? "table" : "create";

		Schema::$func("magazines", function (Blueprint $table) use ($columns, $indexes) {
			!in_array("magazine_id", $columns) && $table->integer("magazine_id")->autoIncrement();
			!in_array("series_name", $columns) && $table->longText("series_name")->nullable();
			!in_array("series_label", $columns) && $table->longText("series_label")->nullable();
			!in_array("issue_id", $columns) && $table->integer("issue_id")->nullable();
			!in_array("issue_description", $columns) && $table->longText("issue_description")->nullable();
			!in_array("icon_url", $columns) && $table->longText("icon_url")->nullable();
			!in_array("store_name", $columns) && $table->longText("store_name")->nullable();
			!in_array("magazine", $columns) && $table->longText("magazine")->nullable();
		});
	}

	/**
	 * Reverse the migrations.
	 *
	 * @return void
	 */
	public function down()
	{
		Schema::dropIfExists("magazines");
	}

	/**
	 * Collect all columns that are indexed in the magazines table.
	 *
	 * @return array
	 */
	private function collectIndexedColumns(): array
	{
		if (!Schema::hasTable("magazines")) {
			return [];
		}

		$sm  = Schema::getConnection()->getDoctrineSchemaManager();
		$tbl = $sm->introspectTable("magazines");

		$indexes = array_reduce($tbl->getIndexes(), function ($carry, $index) {
			return array_merge($carry, $index->getColumns());
		}, []);

		return array_values(array_unique($indexes));
	}
}