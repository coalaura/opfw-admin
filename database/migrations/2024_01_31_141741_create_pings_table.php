<?php
// Auto generated by the build:migrations command

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class CreatePingsTable extends Migration
{
	/**
	 * Run the migrations.
	 *
	 * @return void
	 */
	public function up()
	{
		// Make enums work pre laravel 10
		Schema::getConnection()->getDoctrineConnection()->getDatabasePlatform()->registerDoctrineTypeMapping("enum", "string");

		$indexes = $this->collectIndexedColumns();
		$columns = Schema::getColumnListing("pings");

		$func = Schema::hasTable("pings") ? "table" : "create";

		Schema::$func("pings", function (Blueprint $table) use ($columns, $indexes) {
			!in_array("id", $columns) && $table->integer("id")->autoIncrement();
			!in_array("timestamp", $columns) && $table->integer("timestamp")->nullable();
			!in_array("raw_data", $columns) && $table->longText("raw_data")->nullable();
			!in_array("ideal_location", $columns) && $table->longText("ideal_location")->nullable();
			!in_array("ideal_total_clients", $columns) && $table->integer("ideal_total_clients")->nullable();
			!in_array("ideal_average_ping", $columns) && $table->double("ideal_average_ping")->nullable();
			!in_array("ideal_average_ping_low", $columns) && $table->double("ideal_average_ping_low")->nullable();
			!in_array("ideal_average_ping_high", $columns) && $table->double("ideal_average_ping_high")->nullable();
		});
	}

	/**
	 * Reverse the migrations.
	 *
	 * @return void
	 */
	public function down()
	{
		Schema::dropIfExists("pings");
	}

	/**
	 * Collect all columns that are indexed in the pings table.
	 *
	 * @return array
	 */
	private function collectIndexedColumns(): array
	{
		if (!Schema::hasTable("pings")) {
			return [];
		}

		$sm  = Schema::getConnection()->getDoctrineSchemaManager();
		$tbl = $sm->introspectTable("pings");

		$indexes = array_reduce($tbl->getIndexes(), function ($carry, $index) {
			return array_merge($carry, $index->getColumns());
		}, []);

		return array_values(array_unique($indexes));
	}
}