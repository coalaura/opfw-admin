<?php
// Auto generated by the build:migrations command

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

class CreateExclusiveDealershipRotationTable extends Migration
{
	/**
	 * Run the migrations.
	 *
	 * @return void
	 */
	public function up()
	{
		// Make enums work pre laravel 10
		Schema::getConnection()->getDoctrineConnection()->getDatabasePlatform()->registerDoctrineTypeMapping("enum", "string");

		$indexes = $this->collectIndexedColumns();
		$columns = Schema::getColumnListing("exclusive_dealership_rotation");

		$func = Schema::hasTable("exclusive_dealership_rotation") ? "table" : "create";

		Schema::$func("exclusive_dealership_rotation", function (Blueprint $table) use ($columns, $indexes) {
			!in_array("vehicle_id", $columns) && $table->integer("vehicle_id")->autoIncrement();
			!in_array("model_name", $columns) && $table->longText("model_name")->nullable();
			!in_array("package_name", $columns) && $table->longText("package_name")->nullable();
			!in_array("rotation_day", $columns) && $table->integer("rotation_day")->nullable();

			!in_array("rotation_day", $indexes) && $table->index("rotation_day");
		});
	}

	/**
	 * Reverse the migrations.
	 *
	 * @return void
	 */
	public function down()
	{
		Schema::dropIfExists("exclusive_dealership_rotation");
	}

	/**
	 * Collect all columns that are indexed in the exclusive_dealership_rotation table.
	 *
	 * @return array
	 */
	private function collectIndexedColumns(): array
	{
		if (!Schema::hasTable("exclusive_dealership_rotation")) {
			return [];
		}

		$sm  = Schema::getConnection()->getDoctrineSchemaManager();
		$tbl = $sm->introspectTable("exclusive_dealership_rotation");

		$indexes = array_reduce($tbl->getIndexes(), function ($carry, $index) {
			return array_merge($carry, $index->getColumns());
		}, []);

		return array_values(array_unique($indexes));
	}
}